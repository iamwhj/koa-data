{
  "name": "<%= packageName %>",
  "version": "1.0.0",
  "description": "this is koa server",
  "main": "index.js",
  "dependencies": {
    "koa": "^2.13.0"
  <% if (router) { %>
    ,"koa-bodyparser": "^4.3.0"
  <% } %>
  <% if (router) { %>
    ,"koa-router": "^9.4.0"
  <% } %>
  <% if (mongodb) { %>
    ,"mongoose": "^5.10.10"
  <% } %>
  <% if (panel) { %>
    ,"koa-static": "^5.0.0"
  <% } %>
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "kstart": "nodemon index.js"
  },
  "author": "",
  "license": "ISC"
}
